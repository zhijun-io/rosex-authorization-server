# Security Policy

## 支持范围

`rosex-authorization-server` 仅用于**本地开发与集成测试**，**不是**生产级身份提供方。

请勿：

- 在生产或公网环境部署本服务
- 将默认用户 / client 密钥用于真实系统
- 依赖本项目的密码编码、密钥管理或会话存储作为安全基线（默认使用明文口令编码器等开发友好设置）

## 报告漏洞

若发现可影响使用者本地环境、CI、或下游误用风险的问题，请通过 GitHub Security Advisory 私密报告，或联系维护者（仓库 Issues / org 渠道），**不要**在公开 Issue 中贴出可利用细节。

报告时请尽量包含：

- 受影响版本 / commit
- 复现步骤
- 预期影响范围

我们会确认后在修复版本的 `CHANGELOG.md` 中记录，并视情况发布安全公告。

## 披露时间

确认有效漏洞后，我们会尽快给出修复或缓解建议；具体时间表视严重程度与影响面而定。
