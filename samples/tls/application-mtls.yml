# Spring Boot mTLS overlay: require a client certificate trusted by client-ca.crt.
# Generate materials with: ./scripts/generate-tls-certs.sh
#
# Example:
#   java -jar rosex-authorization-server.jar \
#     --config=samples/config.yml \
#     --spring.config.additional-location=optional:file:samples/tls/application-mtls.yml

spring:
  ssl:
    bundle:
      pem:
        server:
          keystore:
            certificate: samples/tls/certs/server.crt
            private-key: samples/tls/certs/server.key
          truststore:
            certificate: samples/tls/certs/client-ca.crt

server:
  ssl:
    bundle: server
    client-auth: NEED
