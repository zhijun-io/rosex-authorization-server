# Spring Boot TLS overlay for the Authorization Server (server-side HTTPS only).
# Pair with PEM files generated by scripts/generate-tls-certs.sh
#
# Example:
#   java -jar rosex-authorization-server.jar \
#     --config=samples/config.yml \
#     --spring.config.additional-location=optional:file:samples/tls/application-tls.yml

spring:
  ssl:
    bundle:
      pem:
        server:
          keystore:
            certificate: samples/tls/certs/server.crt
            private-key: samples/tls/certs/server.key

server:
  ssl:
    bundle: server
    client-auth: NONE
